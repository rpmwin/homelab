apiVersion: v1
kind: Service
metadata:
  name: backend-2-service
  labels:
    app: backend-2
spec:
  selector:
    app: backend-2
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP